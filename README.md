# CS_474_Proj1
## Code Running Instructions
To run the code for any of the sections, simply open the file with the label of the desired section to test (for example, to test image sampling, open image_sampling.py), and run the file. 